name: Keep Backend Alive

on:
  schedule:
    - cron: '*/5 * * * *' # 每5分钟运行一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://what-we-do.onrender.com/health
